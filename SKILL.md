---
name: tkc-kpi-slide
description: TKC形式の科目別月次推移表（科目残高データ）から、経営会議用のKPIサマリーをPowerPoint（16:9ワイド、ネイティブグラフ、コメント欄付き）にまとめる。受注高・売上高などの月次実績データが揃っていれば併せて取り込み、無ければ売上高・限界利益率・労働分配率だけで組み立てる。同じTKC会計事務所を使う国内グループ各社向けに複数回使う想定で、会社ごとに受注実績データのフォーマットや「受注TOP10」のような固有の指標が違っても対応できるよう作ってある。「経営会議資料」「月次実績」「KPIサマリー」「業績サマリー」をPowerPointで作りたい、他のグループ会社の資料と束ねる前提の資料が欲しい、といった依頼で必ず使う。ユーザーが「TKC」「科目残高」「固変フラグ」「限界利益率」「労働分配率」などの語を使わなくても、TKC形式らしい構造（科目コード／固変区分／勘定科目名＋月ごとの借方・貸方・残高の3列組）のExcelが添付されていれば対象。1つのTKCファイルに複数事業部・複数部門のデータが含まれる場合（部門コード列がある、シートが縦に連結されているなど）も扱う。
---

# TKC科目残高 → 経営KPIスライド（PowerPoint）

とあるグループ会社向けに実際に作って何度か手直しした資料を土台に、国内グループ各社（同じTKC会計事務所を
使っているため科目残高データのフォーマットは共通だが、受注実績データのフォーマットや会社特有の注目指標は
バラバラ）でも使い回せるように一般化したスキル。単なる見た目のテンプレートではなく、「TKCデータの読み方」
「KPIの計算ルール」「よくある落とし穴」「会社ごとの違いをどう吸収するか」までを含む。

## 全体の流れ

1. **添付ファイルの構造を確認する**（ファイル名では判断しない。会社ごとに列がずれたり部門分割されたりする）
2. **KPIを計算する**（`references/calc-rules.md` のルールに従う）
3. **直近月データの信頼性をチェックする**（下記参照。ここを飛ばさない）
4. **目標値・当月の扱いなど、AIが決めてはいけないことをユーザーに確認する**
5. **`scripts/build_deck.js` 用の data.json を組み立てて実行する**（会社特有の指標は`extraSection`に）
6. **QAする**（`validate.py` → `markitdown` → PowerPoint COM書き出しで目視確認）
7. **命名規則に沿ってファイルを保存し、渡す**

以下、各ステップの勘所。

## 1. 添付ファイルの構造を確認する

- 「科目残高データ」の見分け方：見出し行付近に科目コード（4桁）／固変区分／勘定科目名の列があり、
  その右に借方・貸方・残高が3列1組で月ごとに繰り返す。ファイル名は毎回変わりうるので、
  この構造で判定する。拡張子が`.XLS`（旧バイナリ形式）のこともある——`openpyxl`で開けなければ
  `xlrd`（`pip install xlrd`）で開く
- 「受注実績」的なデータの見分け方：日付（月）の行と、受注高・売上高の行が並んでいる。
  **会社によってレイアウトがまったく違う**（ある会社は月ヘッダーが横一列に並ぶ形、
  他社では別形式になりうる）ので、`extract_tkc_kpis.py` の `parse_order_history()` を
  決め打ちで使わず、まず数行 print して構造を目で見てから列番号を渡す。同じ理由で、
  受注実績データがそもそも提供されない会社もある——その場合は無理に探さず、
  「受注高は今回対象外（データ不足）」として売上高・限界利益率・労働分配率の3指標で組み立てる
  （下記「4-b. 受注データが無い場合」参照）
- 受注実績データはExcelに限らない。**PDFで渡されることもある**——その場合はopenpyxl/xlrdではなく
  `pdf`スキルでテキスト・表を抽出し、月ごとの受注高・売上高を読み取ってから同じ形（`{"YYYY-MM":
  (受注高, 売上高)}`）に整形すればよい。抽出元がExcelかPDFかは以降のKPI計算・data.json組み立てに
  影響しない（数値さえ正しく拾えれば同じ扱いができる）。ただし画像スキャンのPDF（テキスト層が無い）
  はOCR頼みになり精度が落ちる点はユーザーに伝えておくとよい
- **1ファイルに複数部門・複数事業部が入っている場合がある**：科目コード列の左に「部門」列が
  追加されていたり、1シート内に部門ごとの表が縦に連結されていて、部門が変わるたびに
  会社名・月ヘッダー・列見出し行が再掲されたりする。この場合は部門ごとにKPIを別々に計算し、
  **部門ごとに別ファイルとして出力する**（ユーザーに部門構成を確認し、ファイル名にも部門名を
  含める。詳細は`calc-rules.md`「複数部門・複数事業部のTKCファイル」を参照）
- 詳しい読み方・科目コードの範囲は `references/calc-rules.md` を参照

まず `python scripts/extract_tkc_kpis.py <ファイル>` （または openpyxl/xlrdで数行読むだけの
簡単なスクリプト）で先頭数行を確認し、ヘッダー行の位置・データ開始行・科目コード列がどこかを
把握してから本計算に進むこと。TKCの標準フォーマットでも、会社によって微妙にずれることがある。

## 2. KPIを計算する

`references/calc-rules.md` に計算式と厳守事項をまとめてある。要点だけ書くと：

- 売上高＝4111〜4115の当月発生高合計、変動費＝Ｖフラグ科目の合計、限界利益＝売上高－変動費、
  限界利益率＝限界利益／売上高
- 人件費は固定12科目リスト（製造原価側6科目＋販管費側6科目）、労働分配率＝人件費／限界利益（控除法）
- 前月比は金額指標が増減率(%)、比率指標がポイント差(pt)。3ヶ月平均・3ヶ月平均前期間比も同様
- 目標対比は「（実績－目標）／目標」。目標値は必ずユーザーに聞く。AIが推測しない。
  会社によっては「目標金額」らしき列がシート上にあっても、原材料値上げ分の織り込みなど
  前提が違って月次実績と単純比較できないことがある——列があるからと鵜呑みにせず、
  数字の桁感が実績と合っているか確認し、疑わしければユーザーに聞く

`scripts/extract_tkc_kpis.py` の `find_month_columns()` / `compute_monthly_kpis()` を使うと、
月ヘッダーの解析と当月発生高の集計を自動化できる（和暦「R 7. 1」形式のヘッダーに対応。
違う形式なら手動で月→列番号の対応表を作る）。どちらの関数も列番号を引数で渡す設計なので、
部門列が増えて全体が1列右にずれるような会社でも、コードを変更せず引数だけで対応できる。

## 3. 直近月データの信頼性をチェックする（重要・飛ばさない）

実際にこの資料を作ったとき、受注実績データの最新月（28,000／27,500のようなきりの良い数字）が、
科目残高データから計算した実際の売上高（52,340千円）と大きく乖離しているという問題が起きた。
原因は「受注管理表がまだ更新されておらず、概算のプレースホルダー値が入っていた」こと。

同じことが他社・他月でも起こりうる。以下のサインに注意する：
- 最新1〜2ヶ月の数字が不自然にきりが良い（下二桁が00や000）
- 受注実績側の売上高列と、科目残高から計算した売上高が大きく（目安：1割以上）乖離している
- 過去の月はぴったり一致するのに、最新月だけずれている

このサインが出たら、**その月を「当月」として使わず、ユーザーに確認する**。
「◯月のデータは未確定の概算値に見えるので、今回は△月を当月として資料を作りますが
よろしいですか」という形で聞くとよい。なお、こうした未確定月もグラフの参考値としては
表示してよい（傾向を隠さない）。KPIサマリーの「当月」計算だけ除外すれば十分。

逆に、受注実績データの最新月が科目残高の売上高とよく一致していれば（目安：数%以内）、
それは信頼できるデータなので当月として問題なく使ってよい。

## 4. ユーザーに確認すべきこと（AIが決めない）

- 受注高・売上高の目標値（千円）：資料に無ければ必ず聞く。「目標未設定」のままにするか、
  数値を教えてもらうかを選んでもらう
- 当月の扱い：上記3で信頼性に疑義がある場合
- 出力形式：Excel／Word／PowerPointのどれか（このスキルはPowerPointを前提にしている。
  Excelで数式ベースの資料が欲しいと言われたら、このスキルではなく `xlsx` スキルで
  セル数式による集計表を作る）
- 会社特有の指標（受注TOP10、設備稼働率、不良率など）：会社ごとに注目したい指標が違うことが
  多い。データがあり、かつ何を出すか自明でなければユーザーに確認する。詳細は下記「5. 会社特有の
  指標（extraSection）」参照

### 4-a. 受注TOP10・取引先別ランキングについて

取引先別の受注データがあれば集計して出せる（`extraSection`の表として。実例を
`scripts/sample.data.json`に収録）。無ければ「データ不足」として欄自体は残す（消さない）か、
そもそも`extraSection`を省略してKPIカード・チャート・所見・コメント欄だけの構成にする
（`build_deck.js`は`extraSection`が無ければ自動的に所見とコメント欄を広げたレイアウトになる）。

### 4-b. 受注データが無い場合

受注実績データそのものが提供されない会社もある。この場合、無理に受注高を推測せず、
`kpis`を3枚（売上高・限界利益率・労働分配率）、`charts`を2枚（売上高の推移／限界利益率・
労働分配率の推移）に減らして組み立てる。`build_deck.js`はKPIカード・チャートの枚数を
`kpis`/`charts`配列の要素数から自動計算するので、コード側の変更は不要——data.json側で
該当項目を含めなければよい。`dataNote`に「受注実績データが提供されていないため、受注高KPI・
関連チャートは今回対象外」と一言明記する。

## 5. 会社特有の指標（extraSection）

ある会社では得意先別の「受注TOP10」が求められたが、他社では設備稼働率・不良率・部門別採算など
まったく別の指標が求められる可能性が高い。`build_deck.js`はこれを個別にコード対応するのではなく、
**「表」か「箇条書き」を自由に流し込める汎用枠**として`extraSection`に持たせている：

```jsonc
"extraSection": {
  "title": "受注TOP10（得意先別）",
  "subtitle": "2026年5月実績・全得意先合計 31,850千円",   // 省略可
  "layout": "column",   // "column"（既定・1枚目下段に3カラム目として収める）| "page"（2枚目全面）
  "kind": "table",       // "table" | "bullets"
  "table": {
    "columns": [
      { "key": "rank", "label": "順位", "align": "center", "width": 0.32 },
      { "key": "label", "label": "受注先名（工場・部門）", "align": "left" },   // widthを省略した列は残り幅を均等割り
      { "key": "amount", "label": "千円", "align": "right", "width": 0.72 },
      { "key": "share", "label": "構成比", "align": "right", "width": 0.5 }
    ],
    "rows": [ { "rank": 1, "label": "サンプル精機㈱（静岡）", "amount": "3,340", "share": "10.5%" }, ... ]
  },
  // "kind": "bullets" のときは "table" の代わりに "bullets": ["...", "..."] を使う
  "note": "データの出典・期間のズレ・目標値の前提違いなど、注記があれば1〜2文で"
}
```

**layoutの選び方**：既定は`"column"`（1枚目に収める）。ただし表の行数が多い（目安：12行超）、
箇条書きの文章量が多い、あるいは`extraSection`が複数種類必要でどうしても1枚に収まらない場合は、
`"page"`にして2枚目全面のスライドにする——文字を極端に小さくして詰め込むより、2枚に分けて読める
方がよい。判断はClaudeに委ねられているので、実際にQA画像を見て文字サイズ・はみ出しを確認しながら
決めること。`extraSection`自体を省略すれば、所見とコメント欄が2カラムに広がった元のレイアウトに戻る。

`extraSection`が無い＝その会社には特有指標が無い、という意味ではない。単にデータが無い／
今回は間に合わないだけのこともあるので、その場合も`dataNote`か`extraSection.note`相当の
一言（「◯◯データは今回未取得のため対象外」）を残しておくとよい。

## 6. スライドを作る

`scripts/build_deck.js` にKPI・グラフ用データ・所見をJSONで渡すと、`references/design-system.md`
の見た目でPowerPointを生成する。このスクリプトは集計を一切行わない——見た目とレイアウトの
責務だけを持つので、数値はステップ2で計算した実際の値を渡すこと。

```bash
node scripts/build_deck.js data.json 出力ファイル名.pptx
```

`scripts/sample.data.json` が実例（架空のサンプル会社データ、`extraSection`付き）。フィールドの
意味はこのファイルを読めば分かるはずだが、要点：

- `months` / `series`：グラフに使う月次系列データ。`charts`配列で参照する`seriesKeys`が
  存在すればよいので、受注データが無い会社では`orders`/`backlog`系列を単に持たせなければよい
- `kpis`：KPIカードの元データ（3〜4枚。会社によって枚数が変わってよい）。`isRatio: false`
  （金額指標、目標比あり）と`isRatio: true`（比率指標、目標比なし）の2種類がある。目標比・
  前月比・3ヶ月平均前期間比の数式・色分け（良い変化＝緑、悪い変化＝赤）はスクリプト側が
  自動計算するので、差分そのものではなく`value`/`target`/`mom`（前月実績）/`avg3`/`avg3Prev`の
  **生の実績値**を渡す
- `higherIsBetter`：その指標は増えるのが良いか悪いかのフラグ。売上高・受注高・限界利益率は
  `true`、労働分配率は`false`（増えると悪化）
- `insights`：所見の箇条書き（太字リード文＋説明文）。実際に計算した増減傾向だけを根拠に
  3〜4行程度で作る。ここはAIが書いてよい内容だが、**コメント欄（右下の空白ボックス）には
  書き込まない**——これはユーザーが後から手書き・追記するためのスペースとして明示的に
  要望されたもの。この方針は会社が変わっても踏襲する
- `dataNote`：TOP10データ不足・直近月未確定・受注データ欠如などの注記。1〜2文で簡潔に
- `extraSection`：上記「5. 会社特有の指標」参照

事前に `require("pptxgenjs")` が通るか確認すること。通らなければ、`build_deck.js` と同じ
作業ディレクトリで `npm install pptxgenjs` を実行してから再試行する（グローバルには入っていない
環境がある）。

### 依存パッケージが無い場合

- `pptxgenjs`：`npm install pptxgenjs`
- Python側の検証・読み取り用（下記QAステップで使用）：`pip install lxml defusedxml Pillow "markitdown[pptx]" python-pptx pywin32 xlrd`
  （`xlrd`は`.xls`旧バイナリ形式のTKCファイルを読むのに必要になることがある）

## 7. QA（必ず実施する）

pptxスキル（`skills/pptx`）標準の検証手順に、このWindows環境固有の回避策を1つ加える。

1. **構造チェック**：
   ```bash
   PYTHONUTF8=1 python <pptxスキルのパス>/scripts/office/validate.py out.pptx
   ```
   日本語Windows環境では `PYTHONUTF8=1` を付けないと、内部でXMLをcp932として読もうとして
   `illegal multibyte sequence` エラーになることがある。これはこの環境固有の癖で、pptx自体の
   問題ではないので、まずこれを付けて再実行してみること。

2. **内容チェック**：`PYTHONUTF8=1 markitdown out.pptx` でテキスト内容を確認し、数値・所見文が
   意図通りか、文字化けが無いかを見る。

3. **見た目チェック（最重要）**：この機体にはLibreOfficeが入っておらず、pptxスキル標準の
   `scripts/office/soffice.py` は `socket.AF_UNIX` が無いというエラーで必ず失敗する
   （Linuxのサンドボックス回避策がWindowsでは意味を持たないため）。代わりに
   `scripts/export_slides_win32.py` を使う。これは実際にインストールされている
   Microsoft PowerPointをCOM経由で操作してPNG書き出しをするので、Yu Mincho/Yu Gothicなどの
   実フォントで正しくレンダリングされた画像が得られる（LibreOfficeでの代替フォント表示に
   惑わされない）。
   ```bash
   python scripts/export_slides_win32.py out.pptx slide_images
   ```
   まれに`win32com`の型情報キャッシュが壊れて`AttributeError: ... has no attribute
   'CLSIDToClassMap'`のようなエラーになることがある。これはこのファイル固有の問題ではなく
   環境側のキャッシュ破損なので、`python -c "import win32com; print(win32com.__gen_path__)"`
   で出てきたディレクトリを削除してから再実行すれば直る。

   書き出した画像を実際に開いて、以下を目視確認する：
   - テキストがボックスからはみ出していないか（特にKPIカードの4行目・所見の箇条書き・
     `extraSection`の表やコメント欄との重なり）
   - 要素同士が重なっていないか
   - 数値が正しいか（電卓代わりにdata.jsonの数値と見比べる）
   - 文字が小さくなりすぎて実用に耐えないほど詰め込んでいないか——目安として7pt未満が
     多用されている、表の行が省略されている、といった兆候があれば`extraSection.layout`を
     `"page"`にして2枚目に分けることを検討する

   レイアウトを直したら、`build_deck.js`（会社共通のロジック）か`data.json`（会社固有の中身）
   のどちらを直すべきかを見極めて再実行し、もう一度書き出して確認する。1回で決め打ちせず、
   必ず画像で確認すること。

## 8. 保存・命名

ユーザーのグローバル設定で、成果物ファイル名は `YYYYMMDD_ファイル名` 形式が既定になっている。
例：`20260729_サンプル製造株式会社_経営会議資料.pptx`。複数部門を1つのTKCファイルから分割した
場合は、部門名も含める（例：`20260729_サンプルグループ株式会社_A事業部_経営会議資料.pptx`）。
作業用の一時ファイル（data.json、node_modulesなど）は成果物の隣に残さず、スクラッチディレクトリ
側で作業してから最終ファイルだけ本来の保存先にコピーする。

## 参考ファイル

- `references/calc-rules.md` — KPIの計算式・データの読み方・厳守事項・複数部門ファイルの扱い方
- `references/design-system.md` — 配色・フォント・レイアウトの仕様、`extraSection`の詳しい仕様
- `scripts/extract_tkc_kpis.py` — TKCデータの月次集計ヘルパー（ライブラリとして import して使う）
- `scripts/build_deck.js` — スライド生成本体（data.jsonを渡して実行。会社ごとの分岐ロジックは
  持たず、data.json側の内容（kpis/chartsの枚数、extraSectionの有無・layout）に自動追従する）
- `scripts/sample.data.json` — 実例（架空のサンプル会社データ、`extraSection`付き）
- `scripts/export_slides_win32.py` — PowerPoint COM経由のPNG書き出し（この環境でのQA用）
